%h1 Listing topics

%table
  %tr
    %th Name
    %th
    %th
    %th
    %th

  - @topics.each do |topic|
    %tr
      %td= topic.name 
      %td= link_to image_tag(topic.avatar.thumb.url), topic

%br

= link_to 'New Topic', new_topic_path
\|
= link_to 'Home', root_path
